# =====================================================
# 重要：避免敏感資料 / Token 上傳到 GitHub
# 註解必須放在「自己一行」，不能放在 pattern 後面，
# 否則整行（含尾端空白與 #）會被當成 pattern，規則失效
# =====================================================

# === 個人真實資料 ===
# 你的歷史匯入資料（業主名、金額、Excel 原檔）
imports/
# 任何 JSON 備份檔
freelance-backup-*.json
# 備份資料夾
backup/
# 個人化的檔案
*-private.*
# 任何個人資料
local-data/

# === 含 Token 的檔案 ===
.env
.env.local
.env.*.local
config.local.*
secrets.*
credentials.*
# 若你把含真實 Token 的 .gs 存回本地
apps-script-deployed.gs

# === 系統 ===
.DS_Store
Thumbs.db
desktop.ini

# === 編輯器 ===
.vscode/
.idea/
*.swp
*.swo
*~

# === Node（若未來加入打包工具） ===
node_modules/
dist/
package-lock.json

# === 日誌 ===
*.log
npm-debug.log*

# === 暫存 ===
*.tmp
*.cache

# === Claude / AI 協作設定 ===
# 個人協作偏好，不公開
CLAUDE.md

# === 設計素材原檔 ===
# Mascot 原始 SVG / PNG / PSD 等素材（最終版本已 inline 進 js/app.js）
# 原檔保留本機方便日後改色 / 改表情，但不上 GitHub
Mascot/
# 本機開發工具設定（preview launch 等）
.claude/
